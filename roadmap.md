1. -[x] 安装vitepress
2. -[x] 了解vitepress的文件路径系统
3. -[x] 实现src路径映射:这里需要注意，srcDir是从当前目录解析的，别被文档骗了
4. -[] 因为部分文件和文件夹有中文，考虑路由的替换:这里暂时不懂，工作量有点大
5. -[x] 页面展示，侧边栏的折叠,这个其实十分简单，就是配置collapse:false(默认不折叠)就行了。
6. -[x] 动态路由，不可能所有的文件都要靠手工来编辑吧：这里的解决方案不是动态路由，而是在项目运行时将src目录下的文件扫描出来，并生成对应的链接，然后导出到sidebar配置项中
7. -[] 配置github action，后续如果有新的push直接就自动构建了